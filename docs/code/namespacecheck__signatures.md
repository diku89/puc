<!-- Generated by utils/scripts/update_docs.sh. Do not edit directly. -->

# Namespace `check_signatures`

Reject commits that lack an embedded Git commit signature.

This checker verifies signature *presence*, not cryptographic trust. Local Git does not necessarily have the public keys that GitHub knows about, so hooks and CI inspect the raw commit object for its signature header while the GitHub ruleset remains authoritative for verification. Keeping those responsibilities separate gives fast offline feedback without pretending to reproduce GitHub's key and identity policy.

The pre-push path understands Git's four-field hook protocol and checks only commits newly introduced to the selected remote. The direct path accepts normal `git rev-list` expressions for CI and diagnostics.

[Source](../../github/ci_tools/check_signatures.py#L1)

## Variables

<a id="symbol-namespacecheck__signatures_1a96da8c8f091be79e36561ceb5d510f19"></a>

### `SIGNATURE_HEADER_RX`

```cpp
check_signatures.SIGNATURE_HEADER_RX
```

Raw commit header accepted for SHA-1 and SHA-256 object formats.

[Source](../../github/ci_tools/check_signatures.py#L27)

## Functions

<a id="symbol-namespacecheck__signatures_1ae4a5bc31016ba2114c3bdc9e3ef6174d"></a>

### `fail`

```cpp
None fail(str message)
```

Write a consistently prefixed diagnostic to standard error.

**Parameters**

- `message` — Explanation suitable for a hook or CI log.

[Source](../../github/ci_tools/check_signatures.py#L30)

<a id="symbol-namespacecheck__signatures_1a5a3442334ca138c742310731b52e2751"></a>

### `is_null_oid`

```cpp
bool is_null_oid(str oid)
```

Recognize Git's all-zero sentinel without assuming a hash algorithm.

Git installations may use SHA-1 or SHA-256 object IDs. Testing the content rather than a fixed length handles both formats and shortened fixtures.

**Parameters**

- `oid` — Object ID field from Git's hook protocol.

**Returns:** Whether the field is nonempty and contains only zeroes.

[Source](../../github/ci_tools/check_signatures.py#L38)

<a id="symbol-namespacecheck__signatures_1a6c6c82228079bbb08f2948b24d92f5df"></a>

### `revision_commits`

```cpp
list[str] revision_commits(Sequence[str] revisions)
```

Delegate revision-set semantics to Git and return concrete commits.

Passing each expression as its own argument preserves compound sets such as `tip --not --remotes=origin` without invoking a shell.

**Parameters**

- `revisions` — Arguments accepted by `git rev-list`.

**Returns:** Commit object IDs selected by the revision set.

**Throws**

- `subprocess.CalledProcessError` — If Git rejects the revision set.

[Source](../../github/ci_tools/check_signatures.py#L50)

<a id="symbol-namespacecheck__signatures_1ac311532e6a9bc6ddddbfb89cd647024a"></a>

### `commit_has_signature`

```cpp
bool commit_has_signature(str commit)
```

Inspect only commit headers for a standard or SHA-256 signature field.

Partitioning the raw object at its first blank line prevents commit-message text containing `gpgsig` from being mistaken for an actual signature.

**Parameters**

- `commit` — Commit object ID or revision resolvable by `git cat-file`.

**Returns:** Whether the commit header contains an embedded signature.

**Throws**

- `subprocess.CalledProcessError` — If the object cannot be read.

[Source](../../github/ci_tools/check_signatures.py#L67)

<a id="symbol-namespacecheck__signatures_1ada9bc763a6e86234d5e65e76cb735d08"></a>

### `pre_push_revision_sets`

```cpp
list[list[str]] pre_push_revision_sets(Iterable[str] lines, str remote_name)
```

Translate pre-push ref updates into the commits newly reaching a remote.

Updated refs are simple `old..new` ranges. A newly created ref has no old tip, so it is compared with every commit already reachable from that remote; otherwise an existing signed history would be needlessly rechecked. Deleted refs have no local tip and introduce no commits.

**Parameters**

- `lines` — Four-field records supplied to a Git pre-push hook on stdin.
- `remote_name` — Remote whose existing reachability should be excluded.

**Returns:** A list of argument vectors suitable for `git rev-list`.

**Throws**

- `ValueError` — If any hook record does not have exactly four fields.

[Source](../../github/ci_tools/check_signatures.py#L85)

<a id="symbol-namespacecheck__signatures_1a461987c4898d629b90fe9f590ac2df2f"></a>

### `unique_commits`

```cpp
list[str] unique_commits(Iterable[Sequence[str]] revision_sets)
```

Resolve and de-duplicate commits while retaining deterministic order.

One commit may reach several pushed refs. A dictionary is used as an insertion-ordered set so it is inspected and diagnosed only once.

**Parameters**

- `revision_sets` — Revision argument vectors produced for each ref.

**Returns:** Unique commit IDs in first-seen order.

**Throws**

- `subprocess.CalledProcessError` — If a revision set cannot be resolved.

[Source](../../github/ci_tools/check_signatures.py#L119)

<a id="symbol-namespacecheck__signatures_1ab30fc9e424410a9ce8e7eb96bd2560ab"></a>

### `check_commits`

```cpp
int check_commits(Iterable[str] commits)
```

Report every unsigned commit in a batch.

**Parameters**

- `commits` — Commit IDs to inspect.

**Returns:** 0 when all commits contain signatures, otherwise 1.

**Throws**

- `subprocess.CalledProcessError` — If a commit object cannot be read.

[Source](../../github/ci_tools/check_signatures.py#L136)

<a id="symbol-namespacecheck__signatures_1ae1cf400808c777e1c5892bcb1b3a4710"></a>

### `parse_args`

```cpp
argparse.Namespace parse_args(Optional[Sequence[str]] argv=None)
```

Enforce the mutually exclusive hook-input and direct-revision modes.

**Parameters**

- `argv` — Explicit arguments for tests, or `None` for `sys.argv`.

**Returns:** Validated command-line namespace.

[Source](../../github/ci_tools/check_signatures.py#L151)

<a id="symbol-namespacecheck__signatures_1a147a6d6232c2239178c4b8059b4dd923"></a>

### `main`

```cpp
int main(Optional[Sequence[str]] argv=None, Optional[TextIO] stdin=None)
```

Resolve the requested commit set and enforce signature presence.

**Parameters**

- `argv` — Optional command-line arguments.
- `stdin` — Injectable pre-push stream; defaults to process stdin.

**Returns:** 0 for success, 1 for unsigned commits, or 2 for malformed input or a Git inspection failure.

[Source](../../github/ci_tools/check_signatures.py#L176)
